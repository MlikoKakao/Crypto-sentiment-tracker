[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "crypto-sentiment-tracker"
version = "0.1.0"
description = "Crypto sentiment analysis app with Streamlit, FastAPI, and SQLite caching."
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "pandas",
    "numpy",
    "scipy",
    "requests",
    "python-dotenv",
    "pytz",
    "pydantic",
    "statsmodels",
]

[project.optional-dependencies]
api = [
    "fastapi",
    "uvicorn[standard]",
]

ui = [
    "streamlit",
    "plotly",
]

fetchers = [
    "praw",
    "google-api-python-client",
    "feedparser",
]

sentiment = [
    "nltk",
    "textblob",
    "vaderSentiment",
]

benchmark = [
    "scikit-learn",
]

ml = [
    "torch",
    "transformers",
]

dev = [
    "pytest",
    "mypy",
    "pandas-stubs",
    "plotly-stubs",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.mypy]
mypy_path = "stubs"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
